'''yaml

name: Deploy to GitHub Pages

on:

push:

branches:

main

permissions:

contents: read

pages: write

id-token: write

concurrency:

group: "pages" cancel-in-progress: true

jobs:

deploy:

environment:

name: github-pages

url: ${{ steps:

deployment.outputs.page_url }}

runs-on: ubuntu-latest

steps:

name: Checkout

uses: actions/checkout@v4

name: Setup Bun

uses: over

with:

'setup-bun@v1

bun-version: latestname: Install dependencies

run: bun install

4)

name: Build with Lovable script run: bun run build:github

name: Upload artifact

uses: actions/upload-pages

-artifact@v3

with:

path: ./dist

name: Deploy to GitHub Pages id: deployment

uses: actions/deploy-pages@v4
